image: maven:latest

cache:
  paths:
    - .m2/repository/

build:
  stage: build
  script:
    - mvn compile

test:
  stage: test
  script:
    - mvn verify

deploy:
  stage: deploy
  artifacts:
    expire_in: 1 month
  script:
    - mvn clean package -B
    - ls target/classes/
